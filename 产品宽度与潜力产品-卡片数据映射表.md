# 产品宽度 & 潜力产品 — 卡片数据映射表

> 状态说明：✅ 已接入后端 | ⚠️ 部分后端 | ❌ 硬编码/客户端伪数据 | 🔧 待实现

---

## 一、产品宽度页（Product Width）

### 1.1 总览 Tab (`w-overview`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| K1 | 产品宽度 | `w-kpi-avgwidth` | `App.Data.getWidth(team).kpi.avgWidth` | client-side ✅ | `GET /api/width/summary?dept=` | `width_records` | ✅ |
| K2 | 规上用户数 | `w-kpi-scale-users` | `App.Data.getWidth(team).kpi.scaleUsers` | client-side ✅ | `GET /api/width/summary?dept=` | `width_records` | ✅ |
| K3 | 规上客户数 | `w-kpi-scale-customers` | `App.Data.getWidth(team).kpi.scaleCustomers` | client-side ✅ | `GET /api/width/summary?dept=` | `width_records` | ✅ |
| C1 | 人均产品宽度分布 | `chart-w-dist` | `GET /api/width/dist?dept=` | backend ✅ | 已有 | `width_records` | ✅ |
| C2 | 产品宽度 按部门/组 | `chart-w-team-avg` | `GET /api/width/team-avg?dept=` | backend ✅ | 已有 | `width_records` + `groups` | ✅ |
| C3 | 产品覆盖率 | `chart-w-cov` | `GET /api/width/heatmap?dept=` | backend ✅ | 已有 | `width_records` | ✅ |
| H1 | 产品覆盖热力图 | `w-heatmap` | `GET /api/width/heatmap?dept=` → `renderHeatmap()` | backend ✅ | 已有 | `width_records` | ✅ |
| C4 | 人均产品宽度历史趋势 | `chart-w-width-trend` | `App.PERSONS[].aw` 硬编码 | ❌ | `GET /api/width/trend?dept=` | `width_records` | 🔧 |

### 1.2 产品 Tab (`w-product`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| T1 | 产品覆盖率排名（客户） | `#wProdCovCustBody` | `App.Data.getWidth(team).heatmap` → `renderWidthProductTab()` | client-side ⚠️ | `GET /api/width/heatmap?dept=` | `width_records` | ⚠️ |
| T2 | 产品覆盖率排名（用户） | `#wProdCovUserBody` | `App.Data.getWidth(team)` → `renderWidthProductTab()` | client-side ⚠️ | `GET /api/width/heatmap-user?dept=` | `width_records` | 🔧 |
| M1 | 产品交叉销售关联矩阵 | `#w-cross-sell-matrix` | `App.Data.getWidth(team).crossSell` | client-side ⚠️ | `GET /api/width/cross-sell?dept=` | `width_records` | 🔧 |
| B1 | 产品套包组合 | `#w-cross-bundles` | `App.Data.getWidth(team).crossSell.bundles` | client-side ⚠️ | 同上 | `width_records` | 🔧 |

### 1.3 团队 Tab (`w-team`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| T3 | 团队维度对比表 | `#w-team-table` | `App.Data.getWidth(team).teamDimension` | client-side ⚠️ | `GET /api/width/team-dimension?dept=` | `width_records` + `groups` | 🔧 |
| T4 | 团队明细比较 | `#w-team-detail` | `App.WidthDetail` → `App.WidthTeamMatrix` | ⚠️ | 同上 | `width_records` | 🔧 |

### 1.4 客户 Tab (`w-customer`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| K4 | 低宽度客户 KPI | `w-low-count/rate/avg/gap` | `App.Data.getWidth(team).missing` | client-side ⚠️ | `GET /api/width/low-width?dept=&type=cust` | `width_records` | 🔧 |
| C5 | 低宽度客户 部门分布 | `chart-w-low-dept` | `App.Data.getWidth(team)` 衍生 | client-side ⚠️ | 同上 | `width_records` | 🔧 |
| C6 | 低宽度客户 小组分布 | `chart-w-low-group` | `App.Data.getWidth(team)` 衍生 | client-side ⚠️ | 同上 | `width_records` | 🔧 |
| T5 | 优秀客户 Top20 | `#w-tbody-cust-good` | `App.Data.getWidth(team).custGood` | client-side ⚠️ | `GET /api/width/customers?dept=&sort=desc&limit=20` | `width_records` | ⚠️ 已有 |
| T6 | 需提升客户 Bottom20 | `#w-tbody-cust-bad` | `App.Data.getWidth(team).custBad` | client-side ⚠️ | `GET /api/width/customers?dept=&sort=asc&limit=20` | `width_records` | ⚠️ 已有 |
| T7 | 客户数据明细表 | `#w-customer-table` | `App.WidthCustomer.render()` → `App.WidthCustomer.RAW` | ⚠️ | `GET /api/width/customers?dept=` | `width_records` | ⚠️ 已有 |

### 1.5 用户 Tab (`w-user`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| K5 | 低宽度用户 KPI | `wu-low-count/rate/avg/gap` | `App.Data.getWidth(team)` 衍生 | client-side ⚠️ | `GET /api/width/low-width?dept=&type=user` | `width_records` | 🔧 |
| T8 | 用户数据明细表 | `#w-user-table` | `App.WidthUser.render()` → `App.WidthCustomer.RAW` | ⚠️ | `GET /api/width/users?dept=` | `width_records` | 🔧 |

### 1.6 导入 Tab (`w-import`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| K6 | 规上用户/客户计数 Tag | `w-total-user/cust-count` | `App.ImportData.UserGS/CustGS.length` | 内存 | `GET /api/import/width-records` | `width_records` | ✅ |
| T9 | 产品宽度总表 | `#wImportDataTbody` | `App.ImportData.render()` → UserGS/CustGS | 内存 ✅ dept filter | `GET /api/import/width-records?type=` | `width_records` | ✅ |

---

## 二、潜力产品页（Potential Product）

### 2.1 总览 Tab (`p-overview`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| K7 | 总销售额 | `p-kpi-sales` | `App.Data.getPotential(team).kpi` | client-side ⚠️ | `GET /api/potential/summary?dept=` | `potential_cust` | 🔧 |
| K8 | 潜力产品数 | `p-kpi-prodcount` | `App.Data.getPotential(team).kpi` | client-side ⚠️ | 同上 | `potential_cust` | 🔧 |
| K9 | 客户数 | `p-kpi-custcount` | `App.Data.getPotential(team).kpi` | client-side ⚠️ | 同上 | `potential_cust` | 🔧 |
| K10 | 客单价 | `p-kpi-avgprice` | `App.Data.getPotential(team).kpi` | client-side ⚠️ | 同上 | `potential_cust` | 🔧 |
| K11 | 用户数 | `p-kpi-usercount` | `App.Data.getPotential(team).kpi` | client-side ⚠️ | 同上 | `potential_cust` + `potential_user` | 🔧 |
| T10 | 销售额排名表 | `#pOvDeptRank` | `App.Data.getPotential(team).deptRank` | client-side ⚠️ | `GET /api/potential/dept-ranking?dept=&dim=` | `potential_cust` | 🔧 |
| C7 | 销售量构成 | `chart-p-composition` | `App._updateCompositionChart()` → `App.Data.getPotential()` | client-side ⚠️ | `GET /api/potential/composition?dept=` | `potential_cust` | 🔧 |
| C8 | 本期销售额趋势 | `chart-p-yoy` | `App._updateYoyChart()` → `App.Data.getPotential()` | client-side ⚠️ | `GET /api/potential/trend?dept=` | `potential_cust` | 🔧 |
| C9 | 量价四象限诊断 | `chart-p-quadrant` | `App.Data.getPotential(team)` 衍生 | client-side ⚠️ | `GET /api/potential/quadrant?dept=` | `potential_cust` | 🔧 |

### 2.2 产品 Tab (`p-product`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| T11 | 产品覆盖率排名（客户） | `#pProdCovCustBody` | `renderPotentialProductCoverage()` → 客户端 | client-side ⚠️ | `GET /api/potential/product-coverage?dept=&type=cust` | `potential_cust` | 🔧 |
| T12 | 产品覆盖率排名（用户） | `#pProdCovUserBody` | `renderPotentialProductCoverage()` → 客户端 | client-side ⚠️ | `GET /api/potential/product-coverage?dept=&type=user` | `potential_user` | 🔧 |
| C10 | 潜力四象限 覆盖率×增速 | `chart-p-quad2` | `App.Data.getPotential(team)` 衍生 | client-side ⚠️ | `GET /api/potential/quadrant2?dept=` | `potential_cust` | 🔧 |

### 2.3 团队 Tab (`p-team`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| T13 | 差距看板·横向竞争 | `#p-scorecard-body` | `renderTeamScorecard()` → `App.Data.getPotential()` | client-side ⚠️ | `GET /api/potential/scorecard?dept=&dim=&metric=` | `potential_cust` | 🔧 |
| T14 | 增长结构分析 | `#p-growth-struct-grid` | `renderGapDeepDive()` → `App.Data.getPotential()` | client-side ⚠️ | `GET /api/potential/growth-structure?dept=&dim=` | `potential_cust` | 🔧 |
| T15 | 团队×产品 本期vs同期 | `#p-team-cross-tbody` | `App.WidthTeamMatrix.render()` → `App.Data.getPotential()` | client-side ⚠️ | `GET /api/potential/team-matrix?dept=&dim=` | `potential_cust` | 🔧 |
| T16 | 潜力产品覆盖排名 | `#p-seller-table` | `renderSellerPotentialRank()` → client | client-side ⚠️ | `GET /api/potential/seller-ranking?dept=&dim=` | `potential_cust` | 🔧 |

### 2.4 客户维度 Tab (`p-customer`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| T17 | 客户×产品 关联表 | `#pCustUserBody` | `renderCustUserLink()` → client | ⚠️ | `GET /api/potential/customer-link?dept=` | `potential_cust` | 🔧 |

### 2.5 用户维度 Tab (`p-user`)

| # | 卡片 | DOM 元素 | 当前数据源 | 当前 dept | 目标 API | 数据库表 | 状态 |
|---|------|---------|-----------|----------|---------|---------|------|
| T18 | 用户×产品 关联表 | `#pUserCustBody` | `renderUserCustLink()` → client | ⚠️ | `GET /api/potential/user-link?dept=` | `potential_user` | 🔧 |

---

## 三、数据库表映射

| 表名 | 存储内容 | 主要字段 |
|------|---------|---------|
| `width_records` | 产品宽度（用户+客户） | id, tenant_id, record_type, name, dept, group_name, sales, guishang, width, prods_json |
| `potential_cust` | 潜力产品-客户维度 | id, tenant_id, dept2~5, sales, product, cust_name, amount, amount_prev, yoy, qty, … |
| `potential_user` | 潜力产品-用户维度 | id, tenant_id, center, dept3~5, sales, product, user_name, out_amt, out_amt_prev, … |
| `groups` | 组织架构-小组 | id, tenant_id, name, dept_id, leader |
| `departments` | 组织架构-部门 | id, tenant_id, name |
| `product_dict` | 产品字典 | id, name, is_potential |

## 四、后端 API 蓝图

已实现（✅）：
```
GET /api/width/summary?dept=          ← KPI 卡片
GET /api/width/dist?dept=             ← 宽度分布
GET /api/width/team-avg?dept=         ← 团队均值
GET /api/width/heatmap?dept=          ← 覆盖率 + 热力图
GET /api/width/dist/drill?bucket=&dept= ← 分布下钻
GET /api/width/customers?dept=&limit=  ← 客户列表
GET /api/import/width-records?type=    ← 导入数据 CRUD
GET /api/import/potential-cust         ← 潜力客户数据
GET /api/import/potential-user         ← 潜力用户数据
```

待实现（🔧）：
```
GET /api/width/trend?dept=              ← 历史趋势
GET /api/width/heatmap-user?dept=       ← 用户维度覆盖率
GET /api/width/cross-sell?dept=         ← 交叉销售矩阵
GET /api/width/low-width?dept=&type=    ← 低宽度分析
GET /api/width/users?dept=&limit=       ← 用户明细列表
GET /api/width/team-dimension?dept=&dim= ← 团队维度

GET /api/potential/summary?dept=        ← KPI 卡片
GET /api/potential/dept-ranking?dept=&dim= ← 部门排名
GET /api/potential/composition?dept=      ← 产品构成
GET /api/potential/trend?dept=&prods=     ← 销售趋势
GET /api/potential/quadrant?dept=         ← 四象限诊断
GET /api/potential/quadrant2?dept=        ← 覆盖率四象限
GET /api/potential/product-coverage?dept=&type= ← 产品覆盖率排名
GET /api/potential/scorecard?dept=&dim=&metric= ← 差距看板
GET /api/potential/growth-structure?dept=&dim=  ← 增长结构
GET /api/potential/team-matrix?dept=&dim=       ← 团队×产品矩阵
GET /api/potential/seller-ranking?dept=&dim=    ← 销售排名
GET /api/potential/customer-link?dept=          ← 客户关联表
GET /api/potential/user-link?dept=              ← 用户关联表
```

## 五、前端改造策略

已改造（✅）：
| 图表 | 方式 |
|------|------|
| `chart-w-dist` / `chart-w-team-avg` / `chart-w-cov` / `w-heatmap` | `App._widthApiQs()` → 读取 `App.getFilterState('page-width').team` 拼接 `?dept=` → `reloadWidthCharts()` 响应筛选变化 |

待改造（🔧）：
- `chart-w-width-trend` → 新增 `GET /api/width/trend`
- 所有潜力产品图表 → 统一改造为 `await fetch()` + dept 参数
- 所有 KPI 卡片 → 已支持 dept（client-side via `getWidth(team)` / `getPotential(team)`），但可迁移至后端 API 获取更准确数据
