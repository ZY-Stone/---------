# 销售分析一体化平台 - 需求方案 v1.0

## 一、产品定位

将三套分散的内部工具(`index_v13` 潜力分析 / `product_width_analysis` 产品宽度 / `sales-analysis-tool` 综合分析)合并为一个 **多租户SaaS化的销售分析一体化平台**,支持:
- **统一登录**、**多租户隔离**、**角色权限**
- **数据总览 → 产品宽度 → 潜力产品** 的多维分析
- **AI 智能分析**(8模型+3档Prompt+自定义要求)

---

## 二、信息架构(IA)

```
销售分析一体化平台
├── 登录页  /login
├── 总览页  /overview          (FR-1)
├── 产品宽度 /product-width    (FR-2)
│   ├── 总览分析(全平台宽度)
│   ├── 团队维度(团队排名)
│   ├── 账号维度(CRM账号排名)
│   ├── 分组对比(团队vs团队 / 团队vs均值 / 个人vs均值)
│   ├── 历史趋势(宽度均值+覆盖率时间序列)
│   ├── 问题与建议(诊断+模板建议+AI)
│   └── 数据导入(双期XLSX/CSV)
├── 潜力产品 /potential         (FR-3)
│   ├── 产品维度(潜力产品列表+四象限)
│   ├── 客户维度(客户x潜力产品矩阵)
│   ├── 用户维度(行业+最终用户)
│   ├── 销售/团队排名
│   ├── 历史趋势(月度/季度)
│   ├── 问题与建议(三策略+AI)
│   └── 数据导入
├── AI分析    /ai               (统一AI配置+历史分析记录)
├── 账号管理  /admin            (FR-4)
│   ├── 租户管理(仅超级管理员)
│   ├── 用户管理(本租户用户CRUD)
│   ├── 角色与权限
│   ├── 产品字典(27品类)
│   ├── 规上阈值配置
│   └── 审计日志
└── 个人中心  /me               (头像/密码/AI Key/自定义要求)
```

---

## 三、页面结构与功能设计

### 3.1 数据总览 `/overview`

**核心目标**: 用户登录后第一眼看到本租户的全景。

| 区块 | 内容 | 数据来源 |
|------|------|----------|
| 顶部筛选条 | 时间范围、团队、账号(按角色过滤可选) | 全局 |
| KPI条(6卡) | 销售额 / 客户数 / 产品数 / 客单价 / 潜力占比 / 同比% | index_v13 + sales_tool |
| 预警条 | 异常下滑(红)/ 突破空白(橙)/ 快速增长(绿) | index_v13 |
| 矩阵图1×3 | 产品宽度分布 / 团队平均宽度TOP10 / 产品覆盖率TOP10 | product_width |
| 双期对比卡 | 基线 vs 当前 关键指标(可关闭) | sales_tool |
| 2入口卡 | 跳转产品宽度 / 潜力产品 | 新增 |
| 右下浮动按钮 | 导出PDF总览报告 | product_width |

### 3.2 产品宽度 `/product-width`

**继承自 product_width_analysis** + **index_v13 的级联下钻** + **sales_tool 的双期对比**。

```
顶部:  [二级部门 ▾] > [大部门 ▾] > [团队小组 ▾] > [CRM账号 ▾] > [产品 ▾]    [导出PDF]

子Tab:  [总览] [团队维度] [账号维度] [分组对比] [历史趋势] [建议分析] [数据导入]
```

| 子Tab | 内容 | 关键算法 |
|------|------|----------|
| 总览 | KPI卡 + 27品类宽度分布柱图 + 规上vs非规上双图 + 覆盖率热力图 | 宽度=27品类0/1加和;规上=交易额>阈值 |
| 团队维度 | 团队排名表(客户数/平均宽度/规上数/规上率)+ 团队内明细 | product_width |
| 账号维度 | CRM账号排名(客均宽度)+ 关联客户列表 | product_width |
| 分组对比 | 团队A vs 团队B / vs 全部均值 / 个人vs团队均值,雷达图+差异表+自动建议 | product_width |
| 历史趋势 | 月度/季度宽度均值 + 覆盖率折线 + TOP5品类时间序列 | 新增 |
| 建议分析 | 7个行业团队模板 + 个人5步骤建议 + **AI智能分析** | product_width + sales_tool AI |
| 数据导入 | 拖拽/粘贴/XLSX/CSV,基线+当前双期,字段映射向导 | sales_tool |

### 3.3 潜力产品 `/potential`

**继承自 index_v13**(核心)+ sales_tool(双期+AI)。

```
顶部:  [团队▾] [时间▾] [产品▾] [四象限分类▾]    [导出报告]

子Tab:  [产品维度] [客户维度] [用户维度] [排名对比] [历史趋势] [问题与建议] [数据导入]
```

| 子Tab | 内容 |
|------|------|
| 产品维度 | 潜力产品列表(销售额/同比/覆盖) + **量价四象限散点图** + 增长/下滑/新增标签 |
| 客户维度 | 客户x潜力产品交叉矩阵(5级着色) + 售达方依赖度 |
| 用户维度 | 行业分布(11行业) + 最终用户TOP20 |
| 排名对比 | 销售员潜力产品贡献排名 + 团队排名 + 双期进步/退步Top |
| 历史趋势 | 潜力产品月度销售 + 同比曲线 + 覆盖率时序 |
| 问题与建议 | 增长/下滑/新增 三类策略 + **AI智能分析** |
| 数据导入 | 同FR-2.9 |

### 3.4 账号管理 `/admin`   ★ 全新

```
左侧菜单:  [用户管理] [角色权限] [产品字典] [业务参数] [审计日志] [租户设置(超管)]
```

| 模块 | 功能 |
|------|------|
| **用户管理** | 列表(姓名/角色/部门/状态/最后登录)+ 新建/编辑/停用/重置密码/批量导入 |
| **角色权限** | 5种角色预设(可微调)+ 数据范围(全部/本部门/本团队/本人) |
| **产品字典** | 27品类CRUD(品类名/别名/行业归属/权重) |
| **业务参数** | 规上阈值(默认10万)、行业关键词(11行业)、AI默认模型 |
| **审计日志** | 操作类型/操作人/时间/IP/结果,可导出Excel |
| **租户设置** | 租户名/Logo/SSO/默认权重(仅超管) |

---

## 四、权限模型 (RBAC + 数据范围)

### 4.1 角色矩阵

| 角色 | 数据范围 | 关键能力 |
|------|----------|----------|
| super_admin | 全部租户 | 全部权限 |
| tenant_admin | 本租户全部 | 账号管理/字典/参数/审计查看 |
| dept_lead | 本部门 | 全部Tab+导出+AI |
| team_lead | 本团队 | 全部Tab+导出+AI |
| sales | 本人(自己负责的客户/产品) | 全部Tab(只看自己数据)+ AI(自己数据) |
| analyst | 本租户(只读) | 全部Tab+AI |

### 4.2 数据隔离机制

```
所有数据查询 SQL:
  SELECT * FROM sales_data
  WHERE tenant_id = :current_tenant
    AND (
      :role = 'super_admin'
      OR :role = 'tenant_admin'
      OR (:role = 'dept_lead' AND dept_id = :user_dept_id)
      OR (:role = 'team_lead' AND team_id = :user_team_id)
      OR (:role = 'sales' AND owner_id = :user_id)
    )
```

- **租户级**:数据库 `tenant_id` 字段 + 应用层强制注入(不可被前端绕过)
- **角色级**:JWT Token 携带 role + scope,后端中间件校验
- **字段级**:脱敏(手机号/身份证)由后端按角色控制

### 4.3 API Key 加密

- 用户填入的 DeepSeek/OpenAI 等 Key → AES-256 加密 → 存 DB
- 调用时解密 → 调大模型 → 立即丢弃明文
- 超级管理员可重置任一用户的 Key

---

## 五、AI 智能分析方案

### 5.1 支持模型(继承 sales_tool)

| 模型 | Endpoint | 备注 |
|------|----------|------|
| DeepSeek V3 / R1 | `api.deepseek.com/v1` | 默认推荐 |
| 通义 Qwen-Max/Plus | `dashscope.aliyuncs.com` | 阿里云 |
| 豆包 Seed 2.0 Lite/Mini/Pro | `ark.cn-beijing.volces.com` | 字节 |
| OpenAI GPT-4o | `api.openai.com/v1` | 国际 |
| 自定义 | 任意 OpenAI 兼容 | 企业内部代理 |

### 5.2 Prompt 设计

**角色**: 专业销售数据分析师

**输入变量**:
- 当前日期(实时,防幻觉)
- 维度(产品宽度/潜力产品/综合)
- 核心指标(总额/覆盖/排名/同比/客户/团队)
- 用户角色与数据范围(避免泄漏越权数据)
- 个人自定义要求(从 `users.custom_prompt` 读取)

**输出结构**(Markdown):
```markdown
# 一、现状概述
- 销售额/客户数/产品数/同比

# 二、问题诊断
- 关键问题(数量同比×金额同比 四象限分类)
- 覆盖率差距
- 客户/团队异动

# 三、对比分析
- 与团队均值差距
- 与同部门其他团队对比
- 双期变化(进步/退步Top)

# 四、改善行动
- 优先级P1/P2/P3行动项
- 关键客户突破建议
- 关键产品推广策略

# 五、风险提示
```

### 5.3 3档Prompt风格

| 风格 | 长度 | 适用 |
|------|------|------|
| professional | 1500-2000字 | 月度复盘、汇报材料 |
| brief | 300-500字 | 晨会、周会 |
| actionable | 800-1200字,行动导向 | 一线销售日常 |

### 5.4 触发方式

- **手动**: 点击"AI分析"按钮
- **自动**: 首次进入综合Tab 7秒后自动触发一次
- **定时**: 租户可配置每周一早8点生成上周AI周报(预留)

---

## 六、技术架构

### 6.1 整体

```
┌─────────────────────────────────────────────────────────┐
│  前端 (Vue3 + Vite + Element Plus + ECharts)            │
│  - 路由:vue-router  - 状态:Pinia  - HTTP:axios         │
│  - 图表:ECharts 5.x  - Excel:xlsx  - PDF:jspdf         │
└──────────────────┬──────────────────────────────────────┘
                   │ HTTPS / JWT
┌──────────────────┴──────────────────────────────────────┐
│  后端 (FastAPI / Spring Boot)                           │
│  - 鉴权中间件(JWT + RBAC)                              │
│  - 多租户中间件(tenant_id 注入)                        │
│  - AI 网关(模型路由 + 加密Key)                         │
│  - 数据服务(导入/导出/查询)                            │
└──────────────────┬──────────────────────────────────────┘
                   │
   ┌───────────────┼───────────────┐
   │               │               │
┌──┴──┐      ┌─────┴─────┐    ┌────┴────┐
│MySQL│      │ ClickHouse│    │  Redis  │
│账号 │      │ 销售明细  │    │ 缓存    │
└─────┘      └───────────┘    └─────────┘
```

### 6.2 数据表设计(关键)

- `tenants` (id, name, logo, status, created_at)
- `users` (id, tenant_id, name, role, dept_id, team_id, status, encrypted_key, custom_prompt)
- `product_dict` (id, tenant_id, name, alias, industry_id, weight)
- `industry_keywords` (tenant_id, industry, keywords_json)
- `sales_data` (id, tenant_id, period, customer, product, amount, owner_id, team_id, dept_id, is_regulated)
- `ai_history` (id, user_id, scope, model, prompt_hash, output, created_at)
- `audit_log` (id, tenant_id, user_id, action, target, ip, ts)

---

## 七、实施路线(分3期)

| 阶段 | 周期 | 交付 |
|------|------|------|
| **P1 核心** | 4周 | 登录/权限/账号管理 + 数据总览 + 产品宽度(基础) + 潜力产品(基础) |
| **P2 增强** | 3周 | 产品宽度(团队/账号/对比/趋势) + 潜力产品(四象限/双期) + AI接入 |
| **P3 完善** | 3周 | PDF/Excel 导出 + 审计日志 + 性能优化 |

---

## 八、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 多租户数据泄露 | 高 | 中间件强制注入 tenant_id;定期安全审计 |
| AI Key 泄露 | 高 | AES-256 加密;调用后即销毁明文 |
| Prompt 注入 | 中 | 后端白名单;用户输入做转义;输出内容审计 |
| 数据导入格式差异 | 中 | 字段映射向导+模板下载+历史导入记录 |
| 性能(10万行) | 中 | ClickHouse 列存;前端虚拟滚动;ECharts sampling |
