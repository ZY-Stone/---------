## 通用开发提示词：数据隔离与动态计算

请对项目中的数据展示模块做以下改造，使其基于导入数据动态计算，并严格遵循页级筛选层级。

### 一、数据源字段映射
- 检查数据同步函数（如 `syncToRaw`）中 RAW 数据的字段映射是否正确
- 确保聚合所需的字段（如小组名、部门名、销售人员、数据月份）都从导入数据正确映射到 RAW 条目上
- 如果字段名与聚合逻辑中的查找键不匹配（如 `team` 映射了部门名但聚合按组名查找），需要修正映射关系
- 产品列表等维度表头应从导入数据动态同步，不要写死

### 二、数据隔离方法
从原始数据取数，按以下优先级逐级筛选后返回：
  1. 时间范围（如月份/日期筛选器）
  2. 最细粒度（如人员）
  3. 中间粒度（如小组）
  4. 最粗粒度（如部门）
- 某级为「全部」时跳过该级筛选
- 所有聚合函数、下钻弹窗、统计计算统一调用此方法，确保数据范围一致

如果顶部筛选框是选择的是某一个部门，那么该tab里面的所有卡片，容器，下钻，聚合函数等，都是只能是该部门的数据，不能有其他的部门数据

如果直接选择了某一个组，那么该tab里面的所有卡片，容器，下钻，聚合函数等，都是只能是该组的数据，不能有其他的组数据，同时也不能看到该组对应部门的数据

如果直接选择了某一个个人，那么该tab里面的所有卡片，容器，下钻，聚合函数等，都是只能是该个人的数据，不能有其他的个人数据，同时也不能看到该个人对应组的数据



### 四、筛选器联动
- 时间筛选器的 `onchange` 事件追加调用全局刷新函数，确保切换时间后所有模块的数据同步更新
- 刷新函数中应包含：清缓存 → 重新聚合 → 重新渲染表格和图表


### 五、下钻弹窗优化
- 弹窗尺寸根据内容列数动态调整，多列时扩大至 `maxWidth: 95vw; width: 96%`
- 表格使用 `table-layout:auto`，列宽按内容自适应
- 文本列（名称、团队等）使用 `white-space:nowrap` 防止换行截断


### 七、关联模块联动
- 主刷新函数中补充对其他关联模块的刷新调用（如卡片计数、推荐列表等），确保切换筛选后页面所有数据保持一致
